server:
  port: 8080
spring:
  application:
    name: HMoneta
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.1.101:3306/home_lab_server?useUnicode=true;characterEncoding=UTF-8
    hikari:
      username: homelab
      password: HomeLab215178^&%@
    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true
      open-in-view: false
    task:
      scheduling:
        pool:
          size: 1
        thread-name-prefix: HomeLabScheduleTask
logging:
  level:
    sql: info

